spring:
  application:
    name: discovery-service
  cloud:
    config:
      uri: http://myhost:8888
#      fail-fast: true

eureka:
  client:
    registerWithEureka: false
    fetchRegistry: false

server:
  port: 8761
